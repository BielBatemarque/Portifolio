export const Header = (itens, logo) => {
    return(
        <header>
            <ul>
                <li>
                   {itens}
                </li>
            </ul>
        </header>
    );
};